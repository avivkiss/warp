A todo list in taskpaper format, try taskpaper from the Mac App store  or install plaintasks in Sublime Text.

General:
	- verification @done
	- recursive transfer with exposed stat in separate thread 
	- error handling @done
		- if file not present @done
	- progress bar
	- tcp mode testing
	- option for symlinks
	- option to not verify @done
	- chroot to users home directory (either in server code or when the daemon is started or add ~ automatically)
